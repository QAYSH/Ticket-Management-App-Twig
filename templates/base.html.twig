<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}TickBase - Ticket Management{% endblock %}</title>
    <link rel="icon" href="/favicon.ico">
    
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Your exact Vue CSS variables and styles -->
    <style>
        :root {
            --background: 0 0% 100%;
            --foreground: 222.2 47% 11%;
            --card: 0 0% 100%;
            --card-foreground: 222.2 47% 11%;
            --primary: 238 76% 58%;
            --primary-foreground: 0 0% 100%;
            --secondary: 220 14% 96%;
            --secondary-foreground: 222.2 47% 11%;
            --muted: 220 14% 96%;
            --muted-foreground: 220 9% 46%;
            --accent: 238 76% 58%;
            --accent-foreground: 0 0% 100%;
            --destructive: 0 84% 60%;
            --destructive-foreground: 0 0% 100%;
            --border: 220 13% 91%;
            --input: 220 13% 91%;
            --ring: 238 76% 58%;
            --radius: 0.75rem;
            --status-open: 142 76% 36%;
            --status-in-progress: 38 92% 50%;
            --status-closed: 215 16% 47%;
        }

        .container-constrained {
            max-width: 1440px;
            margin-left: auto;
            margin-right: auto;
            padding-left: 1rem;
            padding-right: 1rem;
        }

        @media (min-width: 640px) {
            .container-constrained {
                padding-left: 1.5rem;
                padding-right: 1.5rem;
            }
        }

        @media (min-width: 1024px) {
            .container-constrained {
                padding-left: 2rem;
                padding-right: 2rem;
            }
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }

        .animate-float-slow {
            animation: float 8s ease-in-out infinite;
        }

        /* Custom color classes using your CSS variables */
        .bg-primary { background-color: hsl(var(--primary)); }
        .text-primary { color: hsl(var(--primary)); }
        .bg-secondary { background-color: hsl(var(--secondary)); }
        .text-muted-foreground { color: hsl(var(--muted-foreground)); }
        .bg-card { background-color: hsl(var(--card)); }
        .border-border { border-color: hsl(var(--border)); }
        .bg-status-open { background-color: hsl(var(--status-open)); }
        .bg-status-in-progress { background-color: hsl(var(--status-in-progress)); }
        .bg-status-closed { background-color: hsl(var(--status-closed)); }
    </style>
</head>
<body class="bg-white text-gray-900 antialiased">
    {% block content %}{% endblock %}
    
            <!-- Toast Container -->
        {% set toastService = app.session.get('toast_service') %}
        {% if toasts is defined %}
            {% include "components/toast.html.twig" %}
        {% endif %}
    
    <!-- JavaScript -->
    <script src="/assets/js/toast.js"></script>
    <script src="/assets/js/app.js"></script>
</body>
</html>